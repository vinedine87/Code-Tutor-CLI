#!/usr/bin/env bash
# Pre-commit hook: scan staged files for secrets/logs/prompts
node scripts/precommit-scan.js
status=$?
if [ $status -ne 0 ]; then
  echo "pre-commit: blocked by scanner"
  exit $status
fi
exit 0

